{
  "name": "levtrade",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && npm run build:signals",
    "build:signals": "npx esbuild src/signals/api-entry.ts --bundle --format=esm --platform=node --outfile=api/_signals.mjs",
    "build:collector": "npx esbuild scripts/run-collector.ts --bundle --format=esm --platform=node --outfile=dist-server/run-collector.mjs && npx esbuild scripts/collector-loop.ts --bundle --format=esm --platform=node --outfile=dist-server/collector-loop.mjs",
    "collector:once": "node dist-server/run-collector.mjs",
    "collector:start": "node dist-server/collector-loop.mjs",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.1",
    "lightweight-charts": "^5.1.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "tailwindcss": "^4.2.1",
    "zustand": "^5.0.11"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.4",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
