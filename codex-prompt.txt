Read AGENTS.md and COLLAB_LOG.md first. Follow the agent coordination protocol.

# Task: Dashboard Consolidation — Unify & Condense Layout

The dashboard has grown through multiple feature passes and now shows the same information in 2-4 places. This task combines overlapping content into unified displays. NO information is removed — only consolidated. Execute the 5 phases in order.

---

## Phase 1: Tighten CSS Spacing (src/index.css only)

All changes are in `src/index.css`. Do NOT change any .tsx files in this phase.

### 1A. Reduce panel padding

Find:
```css
.panel-shell {
  padding: 1rem;
```
Change `padding: 1rem` → `padding: 0.85rem`

Find the `.decision-hero` rule and change its `padding: 1rem` → `padding: 0.85rem`

### 1B. Reduce section gaps

Find:
```css
.workspace-stack,
.rail-stack,
.risk-stack {
```
Change their `gap: 0.7rem` → `gap: 0.6rem`

Find `.dashboard-shell` and change `gap: 0.75rem` → `gap: 0.65rem`

### 1C. Fix expandable section double spacing

Find:
```css
.expandable-section {
  margin-top: 0.65rem;
```
Change `margin-top: 0.65rem` → `margin-top: 0.55rem` and `padding-top: 0.65rem` → `padding-top: 0.4rem`

### 1D. Tighten stat-card and workflow-summary-card padding

Find `.stat-card` and change `padding: 0.65rem 0.75rem` → `padding: 0.55rem 0.65rem`

Find `.workflow-summary-card` and change `padding: 0.7rem 0.8rem` → `padding: 0.6rem 0.7rem`

### 1E. Reduce panel-copy top margin

Find `.panel-copy` and change `margin-top: 0.55rem` → `margin-top: 0.45rem`

### 1F. Add sibling combinator to prevent margin stacking

Add this rule near the `.panel-copy` rule:
```css
.panel-copy + .workflow-summary-grid,
.panel-copy + .stat-grid,
.panel-copy + .setup-card {
  margin-top: 0.45rem;
}
```

Run `npm run build` to confirm 0 errors before continuing.

---

## Phase 2: Compress Descriptive Text

### 2A. src/components/layout/DashboardLayout.tsx

Remove the chart copy paragraph. Delete these lines entirely:
```tsx
            <p className="panel-copy signal-chart-block__copy">
              Keep price action, stretch, and key levels in view while you work through the setup checklist below.
            </p>
```

### 2B. src/components/risk/RiskSection.tsx

Find the `<p className="panel-copy">` that contains "Only use this section after Step 2 says there is an entry..."

Replace the entire paragraph contents with:
```
Verify size, leverage, and liquidation before entering.
```

### 2C. src/components/setup/SetupHistory.tsx

Find the `<p className="panel-copy">` that mentions "This history is saved locally in this browser..."

Replace the entire paragraph contents with:
```
Locally stored. Outcomes scored from 1h candles over the selected window.
```

### 2D. src/components/claims/TrustPanel.tsx

Find the `<p className="panel-copy">` with the ternary for cloudSyncEnabled.

Replace:
- The truthy branch with: `'Syncing across devices via cloud. Local storage acts as cache.'`
- The falsy branch with: `'History is local to this browser. Enable cloud sync for cross-device sharing.'`

Run `npm run build` to confirm 0 errors before continuing.

---

## Phase 3: Strip Duplicate Guidance Cards & Action Strips

### 3A. src/components/market/MarketRail.tsx

Remove the 3-card workflow-summary-grid AND the action-guidance strip. Delete these lines (approximately lines 63-85):

```tsx
      <div className="workflow-summary-grid">
        <article className="workflow-summary-card">
          <div className="workflow-summary-card__kicker">Market read</div>
          ...
        </article>
        <article className="workflow-summary-card">
          <div className="workflow-summary-card__kicker">What to do now</div>
          ...
        </article>
        <article className="workflow-summary-card">
          <div className="workflow-summary-card__kicker">When to continue</div>
          ...
        </article>
      </div>

      <div className={`action-guidance action-guidance--${guidance.tone}`}>
        {guidance.action}
      </div>
```

Keep everything else: StepLabel, panel-header, panel-copy summary, warmup/stale chips, ExpandableSection.

### 3B. src/components/signal/SignalSection.tsx

Remove the 3-card workflow-summary-grid, the reason chips block, AND the action-guidance strip. Delete these lines (approximately lines 53-84):

```tsx
      <div className="workflow-summary-grid">
        <article className="workflow-summary-card">
          <div className="workflow-summary-card__kicker">Direction</div>
          ...
        </article>
        <article className="workflow-summary-card">
          <div className="workflow-summary-card__kicker">What to do now</div>
          ...
        </article>
        <article className="workflow-summary-card">
          <div className="workflow-summary-card__kicker">What to wait for</div>
          ...
        </article>
      </div>

      {guidance.reasons.length > 0 && (
        <div className="decision-hero__reasons">
          ...
        </div>
      )}

      <div className={`action-guidance action-guidance--${guidance.tone}`}>{guidance.action}</div>
```

Then add a compact direction badge inline with the existing status pill. Find the panel-header area:
```tsx
        <span className={`status-pill status-pill--${guidance.tone}`}>{guidance.label}</span>
```

Change it to show the direction badge before the status pill:
```tsx
        <div className="flex items-center gap-2">
          {guidance.directionLabel !== 'NONE' && (
            <span className={`setup-card__direction setup-card__direction--${guidance.directionLabel === 'LONG' ? 'green' : 'red'}`}>
              {guidance.directionLabel}
            </span>
          )}
          <span className={`status-pill status-pill--${guidance.tone}`}>{guidance.label}</span>
        </div>
```

This preserves the direction info (was in the "Direction" card) as a compact pill.

### 3C. src/components/risk/RiskResults.tsx

Remove the action-guidance strip. Delete these lines (approximately lines 71-73):
```tsx
        <div className={`action-guidance action-guidance--${riskGuidance.tone}`}>
          {outputs.hasInputError ? outputs.inputErrorMessage : riskGuidance.action}
        </div>
```

The risk verdict summary paragraph above already conveys the same guidance.

### 3D. src/components/entry/EntryGeometryPanel.tsx

Check if this file has an `.action-guidance` strip. If so, remove it — the entry quality explanation text already conveys the same message.

Run `npm run build` to confirm 0 errors before continuing.

---

## Phase 4: De-duplicate Metrics Across Panels

### 4A. src/components/signal/SignalSection.tsx — Remove 3 duplicate metrics

In the ExpandableSection's `stat-grid`, there are 4 `Metric` components. Remove the last 3 (Crowd positioning/Funding Z-Score, Money flow/OI Delta, Overall agreement/Composite). These all appear with MORE detail in MarketRail's advanced MiniPanels.

Keep ONLY the first one:
```tsx
          <Metric
            label={<JargonTerm term="Z-Score">Price stretch</JargonTerm>}
            value={`${signals.zScore.value.toFixed(2)}\u03C3`}
            tone={signals.zScore.color}
            onActivate={() => setDrawerKind('zScore')}
          />
```

Since there's now only 1 stat, change the wrapper from `<div className="stat-grid">` to a simple `<div>` or keep the grid (it works with 1 item).

### 4B. src/components/claims/TrustPanel.tsx — Remove duplicate setup metadata

Find the stat-grid that shows tracked setups count, pending outcomes, unresolvable, oldest setup, and newest setup. Remove those 5 stat cards — they already appear in SetupHistory's summary section.

Keep only the storage/sync specific stats: storage key, cloud sync status, last cloud sync, retention policy, sync scope, resolution basis.

Also simplify or remove the `useMemo` that computes `summary` object if it only fed those now-removed stats.

### 4C. src/components/chart/PriceChart.tsx — Remove duplicate entry quality badge

Find the entry quality status pill in the chart header area (look for something like `entryQuality` or a status-pill showing entry quality). Remove it. Entry quality is already shown in SetupCard and EntryGeometryPanel.

Make sure to keep the `useSignals` import if it's used for other things (warmup/stale overlays).

Run `npm run build` to confirm 0 errors before continuing.

---

## Phase 5: Slim RiskResults + Enrich SetupCard

### 5A. src/components/risk/RiskResults.tsx — Remove duplicate stat grid

Remove the `workflow-summary-grid--risk` section that shows 4 stat cards (Account hit at stop, R:R, Liquidation safety, Suggested leverage). These lines approximately:

```tsx
        <div className="workflow-summary-grid workflow-summary-grid--risk">
          <Stat label="Account hit at stop" ... />
          <Stat label={<JargonTerm term="R:R">Reward vs risk</JargonTerm>} ... />
          <Stat label={<JargonTerm term="Liquidation">Liquidation safety</JargonTerm>} ... />
          <Stat label="Suggested leverage" ... />
        </div>
```

Keep everything else: panel-header with trade grade, summary paragraph, status pills, validation warnings, ExpandableSection with advanced risk details.

### 5B. src/components/setup/SetupCard.tsx — Add risk context stats

Import the risk hook:
```tsx
import { usePositionRisk } from '../../hooks/usePositionRisk'
```

Inside the SetupCard function (where `setup` is defined and not null), add:
```tsx
const { outputs: riskOutputs, isReady: riskReady } = usePositionRisk()
```

In the existing `stat-grid` (around line 124), add 2 new stat cards after the existing ones:
```tsx
        {riskReady && riskOutputs && (
          <>
            <Stat
              label="Account hit at stop"
              value={`${riskOutputs.lossAtStopPercent.toFixed(1)}%`}
              tone={riskOutputs.lossAtStopPercent < 1 ? 'green' : riskOutputs.lossAtStopPercent < 2 ? 'yellow' : 'red'}
            />
            <Stat
              label="Liquidation safety"
              value={riskOutputs.effectiveImmune ? 'IMMUNE' : `${riskOutputs.liquidationDistance.toFixed(1)}%`}
              tone={riskOutputs.effectiveImmune || riskOutputs.liquidationDistance > 20 ? 'green' : riskOutputs.liquidationDistance > 10 ? 'yellow' : 'red'}
            />
          </>
        )}
```

This makes SetupCard the single consolidated view of the full trade geometry. You'll need to import `formatPercent` if not already imported. Check the existing imports.

Run `npm run build` to confirm 0 errors.

---

## Final Verification

After all 5 phases:

1. Run `npm run build` — MUST be 0 errors, 0 warnings
2. Confirm no console errors in browser dev tools
3. Confirm at desktop (1440px):
   - DecisionHero still shows action + wait cards + reasons — this is the guidance authority
   - MarketRail shows: header with status pill → summary → [advanced expandable with 5 MiniPanels]
   - SignalSection shows: header with direction badge + status pill → summary → SetupCard (now with risk stats) → [advanced expandable with signal breakdown + price stretch + entry geometry]
   - RiskResults shows: trade grade header → verdict summary → status pills → warnings → [advanced expandable with full risk geometry]
   - TrustPanel shows only sync/storage metadata (no setup counts)
   - Chart has no copy text below kicker
4. Confirm at mobile (390px): no overflow, no clipping, single-column stacking works

## IMPORTANT
- Do NOT remove information — only consolidate and relocate
- Do NOT modify DecisionHero.tsx, MenuDrawer.tsx, or TopBar.tsx
- Do NOT change any signal computation, risk math, or store logic
- Do NOT change the ExpandableSection content inside MarketRail (the 5 MiniPanels stay)
- Do NOT reorder the main column layout (Chart → DecisionHero → MarketRail → SignalSection → AnalyticsTabs)
- If the build fails at any phase, STOP and report the errors before continuing
- Add a COLLAB_LOG.md entry at the end documenting all changes made
